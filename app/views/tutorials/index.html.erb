<h1>Listing tutorials</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Status</th>
    <th>Timerquired</th>
    <th>Cohort</th>
    <th>Subject</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tutorials.each do |tutorial| %>
  <tr>
    <td><%= tutorial.name %></td>
    <td><%= tutorial.status_id %></td>
    <td><%= tutorial.timerquired %></td>
    <td><%= tutorial.cohort_id %></td>
    <td><%= tutorial.subject %></td>
    <td><%= link_to 'Show', tutorial %></td>
    <td><%= link_to 'Edit', edit_tutorial_path(tutorial) %></td>
    <td><%= link_to 'Destroy', tutorial, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tutorial', new_tutorial_path %>

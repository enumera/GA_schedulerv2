<%= form_for(@lesson) do |f| %>
  <% if @lesson.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson.errors.count, "error") %> prohibited this lesson from being saved:</h2>

      <ul>
      <% @lesson.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :timerequired %><br />
    <%= f.number_field :timerequired %>
  </div>
   <div class="field">
    <%= f.label :status_id %><br />
    <%= f.collection_select :status_id, Status.where(thing: "lesson"), :id, :statusname %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
